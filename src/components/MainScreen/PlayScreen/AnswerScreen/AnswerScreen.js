import React from 'react';
import classes from '../PlayScreen.module.css';

const answerScreen = (props) => (
    <div className={classes.PlayScreen}>
        <div className={classes.SlideShow}>DONE!</div>
    </div>
);

export default answerScreen;